import CategoryNotePage from '@/components/CategoryNotePage';

const WorkUpdate = () => {
  return (
    <CategoryNotePage
      category="Work Update"
      title="Work Updates"
      description="Document work progress and project updates"
    />
  );
};

export default WorkUpdate;